import { ImageLoadStrategy } from "../types/misc";

export const DEFAULT_HIDE_CATEGORIES: boolean = false;
export const DEFAULT_INFINITE_CARROUSEL: boolean = false;
export const DEFAULT_PERMANENT_GALLERY: boolean = false;

export const DEFAULT_IMAGE_LOAD_STRATEGY: ImageLoadStrategy = "quality";

export const DEFAULT_PREVENT_FULL_SCREEN: boolean = false;
export const DEFAULT_EVENT_PREFIX: string = "cc-webplayer:";
export const DEFAULT_REVERSE_360: boolean = false;
